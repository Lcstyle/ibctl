/// Live-only profile — single live trading instance.
amends "../base.pkl"

profile = "live"

deployment {
  tradingMode = "live"
}

compose {
  exposePaperApi = false
  exposePaperCommandServer = false
}
